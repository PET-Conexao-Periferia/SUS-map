export default defineNuxtPlugin(() => {
    try {
        // localStorage.setItem('@vp-guide', false);
        // document.querySelector('.active').click();
    } catch (e) {
        console.log(e)
    }
});
